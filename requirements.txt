requests>=2.31.0
rich>=13.7.0

# curl_cffi imiteert een browser-TLS-fingerprint. sofascore_tracker.py en
# server.py importeren dit al, maar het ontbrak hier — een verse install faalde
# daardoor direct op ImportError.
curl_cffi>=0.6.0

# Alleen nodig voor transfermarkt_poc.py (HTML-parsing).
beautifulsoup4>=4.12.0
lxml>=5.0.0
